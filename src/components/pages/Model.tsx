import {useParams} from "react-router-dom";


export const Model = () => {
    const params = useParams();
    console.log(params.id)
    return (
        <div>
            fdsfsd
        </div>
    );
};
